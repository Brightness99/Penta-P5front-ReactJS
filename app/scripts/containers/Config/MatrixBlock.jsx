// @flow

import React from 'react';

type Props = {
  locale: {},
};

export default class MatrixBlock extends React.Component {
  static props: Props;

  render() {
    return (
      <section className="app__config__matrix">
        2
      </section>
    );
  }
}
